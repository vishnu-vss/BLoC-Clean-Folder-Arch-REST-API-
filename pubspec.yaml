name: bloc_clean_arch_with_rest_api
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
    
  dio: ^5.11.1
  equatable: ^3.0.0
  flutter_bloc: ^9.1.1
  go_router: ^18.0.1
  pretty_dio_logger: ^1.4.0
  fpdart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
